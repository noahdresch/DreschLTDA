/**
 * Suporte reutilizável à exportação tabular (seleção de colunas, etc.).
 * Independente de entidade: cada tela monta sua {@link ExportColumnSelection}.
 */
package com.onboardingrsd.empresas.export;
